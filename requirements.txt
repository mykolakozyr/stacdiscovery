streamlit==1.30.0         # Modern, stable – lots of new features
protobuf==4.25.3          # Required for new Streamlit versions
pandas==2.2.1             # Compatible with Streamlit + GeoPandas
geopandas==0.14.3         # Up-to-date & stable
keplergl==0.3.2           # No change (latest)
Shapely==2.0.3            # Required for newer GeoPandas
pystac==1.9.0             # Latest stable (or >=1.5 for full STAC v1)
pystac-client==0.7.5      # Compatible with STAC API spec 1.0.0+
